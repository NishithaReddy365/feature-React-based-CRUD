{
  "name": "react-crud-schema",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "npx vite preview",
    "mock-api": "npx json-server --watch db.json --port 4000"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@mui/material": "^5.15.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "json-server": "^0.17.3"
  }
}
